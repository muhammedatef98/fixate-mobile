/**
 * sentryService.ts — Sentry initialisation stub.
 * Replace the body with a real Sentry.init() call once
 * @sentry/react-native is installed and configured.
 */
export function initSentry(): void {
  // No-op stub — wire up Sentry here when ready.
}
