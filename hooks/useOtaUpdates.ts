/**
 * useOtaUpdates.ts — OTA update hook stub.
 * Replace with expo-updates logic when needed.
 */
import { useEffect } from 'react';

export function useOtaUpdates(): void {
  useEffect(() => {
    // No-op stub — add expo-updates check here when ready.
  }, []);
}
